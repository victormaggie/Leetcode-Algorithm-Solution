#include<cstdio>

using namespace std;

int main() {
	
	int a, b, c, d;
	// scan the 'Enter' and 'blank space';
	scanf("%d%d%d%d", &a, &b, &c, &d);
	printf("DIFERENCE = %d\n", a * b - c * d);
	return 0;
	
}

